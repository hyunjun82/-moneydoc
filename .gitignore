# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Claude Code worktrees 등은 무시하되, 훅 등록(settings.json)은 저장소에 둔다.
# 훅이 게이트를 강제하는 장치라 체크아웃마다 있어야 한다. 폴더째 제외하면 안의 파일을 되살릴 수 없어 * 로 쓴다.
.claude/*
!.claude/settings.json
.playwright-mcp/

# Preview HTML mockups (디자인 시안, 폐기 예정)
moneydoc-home-preview.html
moneydoc-calculator-preview.html
components/GenericCalculator.tsx.backup

# 루트 임시 스크린샷 (자동푸시 오염 방지)
/*.jpeg
/*.jpg
/*.png

# 로컬 글 미리보기 (배포 제외)
/public/_preview/

# 근거 캡처(PNG)는 용량이 커서 제외, JSON 텍스트는 커밋
scripts/article-template/evidence/**/*.png

# 검증 리포트는 실행할 때마다 새로 생기는 산출물이라 커밋하지 않는다
scripts/verify-system/reports/
